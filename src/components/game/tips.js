import React from 'react';
import { getIcons } from '../../data/icons';
import styles from './game.module.scss';

export default function Tips() {
  return (
    <div className={styles.tips}>
      <span style = {styles.h1}></span>
    </div>
  )
}